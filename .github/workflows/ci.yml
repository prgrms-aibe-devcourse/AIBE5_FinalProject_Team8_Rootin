name: CI - Run Tests on PR

on:
  pull_request:
    branches:
      - main
      - feature
    types: [opened, synchronize, reopened]

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: rootin/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: rootin

      - name: Build
        run: npm run build
        working-directory: rootin

      - name: Run tests
        run: npm test -- --watchAll=false --passWithNoTests
        working-directory: rootin
